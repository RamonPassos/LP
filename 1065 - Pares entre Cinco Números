#include <stdio.h>
 
int main() {
  int matriz[5], count, i;
  count = 0;
  scanf("%d %d %d %d %d",&matriz[0],&matriz[1],&matriz[2],&matriz[3],&matriz[4]);
  for(i=0; i<5; i++){
    if(matriz[i]%2==0){
      count = count +1;
    }
  }
  printf("%d valores pares\n",count);
    return 0;
}
