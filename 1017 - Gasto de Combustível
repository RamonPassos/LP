#include <stdio.h>
 
int main() {
 
    int tempo, velocidade;
    double deslocamento, gasto;
    scanf("%d",&tempo);
    scanf("%d",&velocidade);
    deslocamento=tempo*velocidade;
    gasto=deslocamento/12;
    printf("%.3lf\n",gasto);
 
    return 0;
}
