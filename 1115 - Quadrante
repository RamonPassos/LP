#include <stdio.h>
 
int main() {
  int X,Y;
  scanf("%d %d",&X,&Y);
  if(X>0 && Y>0){
    printf("primeiro\n");
    return main();
  }else if(X>0 && Y<0){
    printf("quarto\n");
    return main();
  }else if(X<0 && Y>0){
    printf("segundo\n");
    return main();
  }else if(X<0 && Y<0){
    printf("terceiro\n");
    return main();
  }else if(X==0 || Y==0){
    return 0;
  }

  
  
  
  return 0;

}
